# Drop dish images here to replace the warm-gradient fallbacks.
# Folder structure expected:
#   public/images/dishes/{dish-id}/hero.jpg
#   public/images/dishes/{dish-id}/step-1.jpg
#   public/images/dishes/{dish-id}/step-2.jpg
#   ...
#
# Files are loaded via <ImageWithFallback>. If a file is missing,
# a warm gradient + glyph fallback renders automatically — the app
# stays visually intact even with no image assets present.
